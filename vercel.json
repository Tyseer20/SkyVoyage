{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/api-server build && cp artifacts/api-server/dist/*.mjs api/",
  "builds": [
    {
      "src": "api/index.mjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.mjs"
    }
  ]
}